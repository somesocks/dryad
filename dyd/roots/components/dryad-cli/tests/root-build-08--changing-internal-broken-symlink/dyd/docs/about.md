
# root-build-08--changing-internal-broken-symlink

This test case verifies that changing internal broken symlink targets in `dyd/assets`, `dyd/secrets`, and `dyd/traits` changes the built stem fingerprint.
