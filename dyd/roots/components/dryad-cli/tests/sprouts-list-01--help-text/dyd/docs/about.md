
# sprouts-list-01--help-text

Validates `dryad sprouts list` help text, basic listing, absolute output, and variant filters in both dryad-go and dryad-sh.

This test case tests that `dryad sprouts list --help` runs successfully and returns help text.
