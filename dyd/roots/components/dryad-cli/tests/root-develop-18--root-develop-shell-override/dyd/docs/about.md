# root-develop-18--root-develop-shell-override

Tests root develop shell override behavior: passing `--shell=bash` runs Bash
for the development shell, executes Bash-specific stdin commands, and applies
save.
