# root-build-22--variant-sprout-fanout-default-all

Ensures `dryad root build` fans out a fully variant root into one sprout stem
dependency per concrete variant, without a default unsuffixed `stem` entry.
