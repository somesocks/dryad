# root-replace-03--preserves-variant-target-selectors

This test case verifies that `dryad root replace` matches qualified old targets syntactically, preserves selectors for unqualified replacements, patches only explicitly specified selector dimensions for qualified replacements, and applies include/exclude/from-stdin filters to source root variants.
