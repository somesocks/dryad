# root-move-08--unpin-preserves-variant-requirements-selector

This test case verifies that `dryad root move --unpin` preserves and rewrites
requirements stored under `dyd/requirements~<descriptor>`.
