This test case verifies that `dryad root move` preserves `dyd/variants` plus
variant selector directories on the moved root, and updates dependent
requirements that are stored in `dyd/requirements~<descriptor>`.
