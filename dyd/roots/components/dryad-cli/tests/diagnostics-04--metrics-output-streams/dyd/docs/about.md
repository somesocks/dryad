# diagnostics-04--metrics-output-streams

Ensures diagnostics metrics output routing works correctly, validating that `output: stdout` writes metrics lines to stdout only and `output: stderr` writes metrics lines to stderr only.
