# garden-prune-07--rebuild-pruned-source-stems

Verifies that two different helper roots can produce the same result stem while keeping different source stems, and that `sprouts wipe` plus `garden prune` does not break a later rebuild that reuses the shared result stem and needs the old helper source stem for provenance.
