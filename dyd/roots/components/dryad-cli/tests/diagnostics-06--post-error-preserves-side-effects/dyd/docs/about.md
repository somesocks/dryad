# diagnostics-06--post-error-preserves-side-effects

Ensures diagnostics post-error injection on `os.link` fails a root build while still preserving expected filesystem side effects from the underlying call.
