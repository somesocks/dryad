# root-move-06--preserves-variant-target-selectors

This test case verifies that `dryad root move` preserves variant selectors in requirement target URLs for:
- requirements copied from the moved root
- requirements rewritten in dependent roots
