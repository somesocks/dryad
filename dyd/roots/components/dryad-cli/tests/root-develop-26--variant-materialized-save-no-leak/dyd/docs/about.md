# root-develop-26--variant-materialized-save-no-leak

Tests that root develop materializes selected variant traits in the workspace,
and that `root develop save` does not leak those synthetic variant trait files
back into the source root.
