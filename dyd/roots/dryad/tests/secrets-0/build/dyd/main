#!/usr/bin/env sh

set -e
# set -x

SRC_DIR=$DYD_STEM

DEST_DIR=$1

# copy the secrets to the built stem so it has access
mkdir -p $DEST_DIR/dyd/secrets
cp --symbolic-link -R $SRC_DIR/dyd/secrets/* $DEST_DIR/dyd/secrets/
